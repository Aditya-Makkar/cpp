#include <iostream>
using namespace std;

int main() {
    int t,hcf,lcm;
    cin>>t
    for (int i = 0; i < t; i++)
    {
        cin>n;
        int a[n];
        for (int j = 0; j < n; j++)
            cin>>a[n];
        for (int k = 0; k < n; k++){}
            for(int l=0;l<n;l{
                //(k,l) pai hai if lcm(a[k],a[l]==hcf[])
                x=a[k];
                y=a[l];
                for (int q=1;q<x;q++){
                    if x%q==0 &&y%q==0{
                        hcf=q;
                    }
                }
                lcm=(a*b)/hcf;
                if hcf==lcm
                count++;
            }
            ans++;
        }
        
        
    }
    
}